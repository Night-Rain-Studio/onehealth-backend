# SQLite for local/no-Docker dev — a single file, no service to install.
DATABASE_URL="file:./dev.db"
JWT_SECRET="change_this_to_a_long_random_string"
CORS_ORIGIN="http://localhost:5173"
PORT=4000
NODE_ENV=development
